server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciadordb
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework.web: DEBUG
